pipeline {
    agent any

    stages {
       stage("initilize") {
           echo "initilize"
       }

       stage("test") {
            echo "test"
       }

       stage("deploy") {
            echo "deploy"
       }

    }
}